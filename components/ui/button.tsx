'use client'

/**
 * Compatibility re-export — app code should import from `@/components/Button`.
 * Kept so shadcn/ui aliases resolve to the same design system.
 */
export { Button } from '../Button'
