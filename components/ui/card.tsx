'use client'

/**
 * Compatibility re-export — app code should import from `@/components/Card`.
 */
export {
  Card,
  CardHeader,
  CardTitle,
  CardDescription,
  CardContent,
  CardFooter,
} from '../Card'
